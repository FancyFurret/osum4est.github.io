module BattleSnake {
    export abstract class GameObject {

        //game: Phaser.Game;

        constructor() {
            //this.game = game;
        }

        create() {};
        update() {};
        render(rendering: Rendering) {};
    }
}
